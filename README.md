# AudioV

AudioV = MIDI â†’ visuals in Blender (procedural), built iteratively.

## Step 1 (current)
- Parse a MIDI file (via `mido`)
- Build a readable piano-roll / "sheet" as 3D note bars
- Save a .blend output (headless-friendly)

## Run (headless)
See `scripts/run_step1_blender.py`.
